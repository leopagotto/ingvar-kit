# IKEA Ingka Design System Registry Configuration
# This registry provides access to official @ingka design system packages
# Available only to IKEA employees with proper authentication

@ingka:registry=https://npm.m2.blue.cdtapps.com

# IKEA CWDS (Component Web Design System) Registry
# This registry provides access to @ingka-group-digital CWDS packages
# Requires GitHub personal access token with read:packages scope

//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
@ingka-group-digital:registry=https://npm.pkg.github.com
