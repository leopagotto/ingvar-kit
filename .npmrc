# IKEA Ingka Design System Registry Configuration
# This registry provides access to official @ingka design system packages
# Available only to IKEA employees with proper authentication

@ingka:registry=https://npm.m2.blue.cdtapps.com
